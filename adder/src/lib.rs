#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }

    #[test]
    fn exploration() {
        assert_eq!(2 + 2, 4);
    }

    #[test]
    fn blow_mind() {
        panic!("at the disco");
    }

    #[test]
    fn add() {
        //assert!(adding(1, 1) != 0);
    }
}
